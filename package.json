{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/server"
  },
  "dependencies": {
    "express": "^4.17.2",
    "express-handlebars": "^6.0.2",
    "mongoose": "^6.1.7"
  }
}
